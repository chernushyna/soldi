import React from 'react';

const AdminLoginPageTemplate = ({  mainContent }) => (
    <div className="admin-template">
        <main>{mainContent}</main>
    </div>
);

export default AdminLoginPageTemplate;